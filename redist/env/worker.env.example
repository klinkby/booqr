# Compartment: worker (cross-tenant scheduled jobs).
# Holds ONLY the booqr_batch (BYPASSRLS) credential. NOT the tenant master
# secret, registry/migrator passwords, or JWT signing material — so a worker
# compromise cannot derive t_<id> passwords or mint tenant JWTs.
Infrastructure__Batch__BatchPassword=changeme_booqr_batch
